{
  "name": "my-portfolio",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
  
  },
  "devDependencies": {
    "vite": "...",
    "vite-plugin-gh-pages": "..."
  }
}
